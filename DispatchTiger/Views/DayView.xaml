<UserControl x:Class="DispatchTiger.Views.DayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:DispatchTiger.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource DarkBackgroundBrush}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="320"/>
        </Grid.ColumnDefinitions>

        <!-- Center: Dispatch Schedule Board (Time and Trucks Grid) -->
        <Border Grid.Row="0" Grid.Column="0" Name="DispatchContentContainer" Background="{StaticResource PanelBackgroundBrush}" RenderTransformOrigin="0.5,0.5">
            <Border.RenderTransform>
                <ScaleTransform x:Name="DispatchContentScaleTransform" ScaleX="1" ScaleY="1"/>
            </Border.RenderTransform>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Truck Headers Row -->
                <Grid Grid.Row="0" Background="#202020" Margin="0,0,0,1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>

                    <ItemsControl ItemsSource="{Binding AvailableTrucks}" 
                                  Grid.Column="0"
                                  Margin="60,0,0,0">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="120" 
                                        Background="#2B2B2B" 
                                        BorderThickness="1,0,0,0" 
                                        BorderBrush="#3A3A3A"
                                        MouseLeftButtonUp="TruckHeader_MouseLeftButtonUp"
                                        Tag="{Binding}"
                                        Cursor="Hand">
                                    <TextBlock Text="{Binding PlateNumber}" 
                                              Style="{StaticResource TruckID}"
                                              Padding="8"
                                              TextAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>

                <!-- Schedule Grid Body -->
                <ScrollViewer Grid.Row="1" Name="CenterDispatchScrollViewer" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Auto" CanContentScroll="False">
                    <Grid VerticalAlignment="Top" HorizontalAlignment="Left">
                        <Grid Name="ScheduleGrid" Background="{StaticResource PanelBackgroundBrush}" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                        <Canvas Name="CurrentTimeMarkerCanvas" IsHitTestVisible="False" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                    </Grid>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Right: Action Panel / Selected Details -->
        <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource ActionPanelBorder}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Action Panel" Style="{StaticResource PanelHeader}"/>

                <!-- Selected Job Info -->
                <StackPanel Grid.Row="1" Margin="15,5">
                    <TextBlock Text="Selected Job:" Style="{StaticResource JobSubtext}"/>
                    <Border Style="{StaticResource JobCardBorder}">
                        <StackPanel>
                            <TextBlock 
                                Text="{Binding SelectedJob.Description}" 
                                TextWrapping="Wrap" 
                                Foreground="{StaticResource LightTextBrush}"
                                FontWeight="SemiBold"
                                MinHeight="40"/>
                            <TextBlock 
                                Text="{Binding SelectedJob.DeliveryAddress}" 
                                FontSize="11" 
                                Foreground="{StaticResource MutedTextBrush}" 
                                TextWrapping="Wrap"
                                Margin="0,8,0,0"/>
                            <TextBlock 
                                FontSize="10" 
                                Foreground="{StaticResource AccentOrangeBrush}"
                                Margin="0,8,0,0">
                                <Run Text="Priority: "/>
                                <Run Text="{Binding SelectedJob.Priority}"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Selected Truck Info -->
                <StackPanel Grid.Row="2" Margin="15,5">
                    <TextBlock Text="Selected Truck:" Style="{StaticResource JobSubtext}"/>
                    <Border Style="{StaticResource JobCardBorder}">
                        <StackPanel>
                            <TextBlock Text="{Binding SelectedTruck.PlateNumber}" FontWeight="Bold" Foreground="{StaticResource LightTextBrush}"/>
                            <TextBlock Text="{Binding SelectedTruck.VehicleType}" FontSize="11" Foreground="{StaticResource MutedTextBrush}"/>
                            <TextBlock 
                                FontSize="10" 
                                Foreground="{StaticResource AccentOrangeBrush}"
                                Margin="0,8,0,0">
                                <Run Text="Driver: "/>
                                <Run Text="{Binding SelectedTruck.Driver.Name}"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <!-- Assign Button -->
                    <Button 
                        Name="AssignButton"
                        Content="ASSIGN JOB" 
                        Command="{Binding AssignJobCommand}"
                        Style="{StaticResource AssignButton}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
